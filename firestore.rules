rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only user profile data is allowed to be stored.
    // No game history, no voting history, no gameplay analytics.
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
